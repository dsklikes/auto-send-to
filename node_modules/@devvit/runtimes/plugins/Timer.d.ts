import { Metadata, TimerMessage, Empty } from '@devvit/protos';
import type { Timer as _Timer } from '@devvit/protos';
export default class Timer implements _Timer {
    Wait(request: TimerMessage, _metadata?: Metadata): Promise<Empty>;
}
//# sourceMappingURL=Timer.d.ts.map