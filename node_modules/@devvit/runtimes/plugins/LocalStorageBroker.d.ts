import { Observable } from 'rxjs';
import type { Metadata } from '@devvit/protos';
import { Cursor, Empty, Message, StreamBroker } from '@devvit/protos';
/**
 * Observable KV storage
 */
declare class LocalStorageBroker implements StreamBroker {
    #private;
    constructor(localStorage: Storage);
    Get(cursor: Cursor, metadata?: Metadata): Observable<Message>;
    Put(message: Message, metadata?: Metadata): Promise<Empty>;
}
export { LocalStorageBroker };
//# sourceMappingURL=LocalStorageBroker.d.ts.map